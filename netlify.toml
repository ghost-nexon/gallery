# netlify.toml – makes /admin serve admin.html without .html

[build]
  publish = "."        # deploy root folder
  command = ""         # no build needed

[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200
  force = true        # override any other rules

# Optional: catch all other paths to index.html (for future SPA-like behavior)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
#   force = true
