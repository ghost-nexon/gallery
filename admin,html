<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comatozze Admin Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
    <script src="assets/js/supabase.js"></script>
    <script src="assets/js/admin.js" defer></script>
</head>
<body class="bg-gray-100 font-sans">
    <header class="bg-white shadow-md p-4 text-center">
        <h1 class="text-3xl font-bold text-gray-800">Comatozze Admin Panel</h1>
    </header>
    <main class="container mx-auto p-4">
        <!-- Login Form -->
        <div id="login-form" class="max-w-md mx-auto bg-white p-6 rounded shadow-md">
            <h2 class="text-2xl font-semibold mb-4">Login</h2>
            <form id="auth-form">
                <input type="email" id="email" placeholder="Email" class="w-full mb-4 p-2 border rounded" required>
                <input type="password" id="password" placeholder="Password" class="w-full mb-4 p-2 border rounded" required>
                <button type="submit" class="w-full bg-blue-600 text-white p-2 rounded hover:bg-blue-700">Login</button>
            </form>
            <p id="auth-error" class="text-red-600 mt-2 hidden"></p>
        </div>

        <!-- Admin Content -->
        <div id="admin-content" class="hidden">
            <!-- Upload Section -->
            <section class="bg-white p-6 rounded shadow-md mb-6">
                <h2 class="text-2xl font-semibold mb-4">Upload Image</h2>
                <form id="upload-form">
                    <input type="file" id="image-file" accept="image/*" class="mb-4" required>
                    <div id="preview" class="mb-4 hidden">
                        <img id="preview-image" class="max-w-xs" src="" alt="Preview">
                    </div>
                    <input type="text" id="title" placeholder="Title" class="w-full mb-4 p-2 border rounded" required>
                    <textarea id="description" placeholder="Description" class="w-full mb-4 p-2 border rounded" required></textarea>
                    <button type="submit" class="bg-green-600 text-white p-2 rounded hover:bg-green-700">Upload</button>
                </form>
                <p id="upload-feedback" class="mt-2"></p>
            </section>

            <!-- Images List -->
            <section class="bg-white p-6 rounded shadow-md">
                <h2 class="text-2xl font-semibold mb-4">Manage Images</h2>
                <div id="images-list"></div>
            </section>
        </div>
    </main>
</body>
</html>
